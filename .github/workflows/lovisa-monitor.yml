name: Lovisa New Monitor

on:
  push:
    branches: [main]
    paths:
      - 'sfera_monitor.py'
      - 'config.example.json'
      - '.github/workflows/lovisa-monitor.yml'
  schedule:
    # GitHub Actions uses UTC. 01:37 UTC = 09:37 China time.
    # Staggered after Sfera, Bijou, and Bershka to reduce state commit conflicts.
    - cron: '37 1 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: lovisa-new-monitor
  cancel-in-progress: false

jobs:
  lovisa:
    name: Lovisa New
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Lovisa monitor
        env:
          WECOM_WEBHOOK: ${{ secrets.WECOM_WEBHOOK }}
          PYTHONIOENCODING: utf-8
        run: python sfera_monitor.py --site lovisa

      - name: Commit updated state
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f state/*.sqlite3 || true
          if git diff --cached --quiet; then
            echo "No state changes to commit."
          else
            git commit -m "Update Lovisa monitor state [skip ci]"
            git push
          fi
